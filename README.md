# UNICA signature generator

## Getting started

To develop the application locally run following scripts:

```
npm i
npm run start
```

Application uses `npm`. 
**Do not use `yarn`!**. It'll be blocked by package.json.

## Used URLs

todo